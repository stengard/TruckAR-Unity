using UnityEngine;
using System.Collections;
using UnityEngine.UI;
using metaio;
public class OverlayW : MonoBehaviour {

	// Use this for initialization
    void Start() {

        
    }
	
	// Update is called once per frame
	void Update () {
        //Vector3 myRelativePosition = Vector3Helper.getRelativePosition(transform, Vector3.zero);

        //Check if marker with COS 1 is trackable
       

       
    }
}
